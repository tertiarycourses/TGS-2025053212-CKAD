# Lab 18 — kubectl top and Metrics Server

`kubectl top` shows real-time CPU and memory for nodes and Pods. It requires the **metrics-server** add-on. CKAD 2026 tests installation of metrics...

**Lab environment:** [KillerCoda](https://killercoda.com/tertiarycourses/course/labs/lab-18-metrics)

A pre-built Kubernetes cluster (controlplane + node01) is ready to use — `kubectl` works immediately.
