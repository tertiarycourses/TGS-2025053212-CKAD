# Lab 15 — DaemonSets and StatefulSets

A DaemonSet runs exactly one Pod per matching node — used for log collectors, CNI agents, and node exporters. A StatefulSet gives Pods stable netwo...

**Lab environment:** [KillerCoda](https://killercoda.com/tertiarycourses/course/killercoda/lab-15-daemonset-statefulset)

A pre-built Kubernetes cluster (controlplane + node01) is ready to use — `kubectl` works immediately.
