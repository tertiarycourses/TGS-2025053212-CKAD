# Lab 2 — Multi-Stage Dockerfile

Separate the build stage from the runtime stage to produce images that are 10× smaller and contain no compiler toolchain. Multi-stage builds are a ...

**Lab environment:** [KillerCoda](https://killercoda.com/tertiary-labs-ckad/course/killercoda/lab-02-multistage-dockerfile)

A pre-built Kubernetes cluster (controlplane + node01) is ready to use — `kubectl` works immediately.
