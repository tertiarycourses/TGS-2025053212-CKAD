# Lab 30 — NetworkPolicy

By default every Pod can communicate with every other Pod. NetworkPolicies restrict that traffic by allow-listing specific sources and destinations...

**Lab environment:** [KillerCoda](https://killercoda.com/tertiarycourses/course/tertiary-labs/lab-30-networkpolicy)

A pre-built Kubernetes cluster (controlplane + node01) is ready to use — `kubectl` works immediately.
